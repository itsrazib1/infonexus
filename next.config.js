/** @type {import('next').NextConfig} */
const nextConfig = {}

module.exports = {
    nextConfig,
    images: {
        domains: ['lh3.googleusercontent.com'],
        domains: ['avatars.githubusercontent.com'],
      },
}
